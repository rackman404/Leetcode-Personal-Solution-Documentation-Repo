
[452. Minimum Number of Arrows to Burst Balloons](https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/)


There are some spherical balloons taped onto a flat wall that represents the XY-plane. The balloons are represented as a 2D integer array `points` where `points[i] = [xstart, xend]` denotes a balloon whose **horizontal diameter** stretches between `xstart` and `xend`. You do not know the exact y-coordinates of the balloons.

Arrows can be shot up **directly vertically** (in the positive y-direction) from different points along the x-axis. A balloon with `xstart` and `xend` is **burst** by an arrow shot at `x` if `xstart <= x <= xend`. There is **no limit** to the number of arrows that can be shot. A shot arrow keeps traveling up infinitely, bursting any balloons in its path.

Given the array `points`, return _the **minimum** number of arrows that must be shot to burst all balloons_.

**Example 1:**

**Input:** points = [[10,16],[2,8],[1,6],[7,12]]
**Output:** 2
**Explanation:** The balloons can be burst by 2 arrows:
- Shoot an arrow at x = 6, bursting the balloons [2,8] and [1,6].
- Shoot an arrow at x = 11, bursting the balloons [10,16] and [7,12].

**Example 2:**

**Input:** points = [[1,2],[3,4],[5,6],[7,8]]
**Output:** 4
**Explanation:** One arrow needs to be shot for each balloon for a total of 4 arrows.

**Example 3:**

**Input:** points = [[1,2],[2,3],[3,4],[4,5]]
**Output:** 2
**Explanation:** The balloons can be burst by 2 arrows:
- Shoot an arrow at x = 2, bursting the balloons [1,2] and [2,3].
- Shoot an arrow at x = 4, bursting the balloons [3,4] and [4,5].

**Constraints:**

- `1 <= points.length <= 105`
- `points[i].length == 2`
- `-231 <= xstart < xend <= 231 - 1`

---
# Input/Output

Input:
- number of Points from 1 to 105
	- xstart and xend given in a list of lists
		- can be -231 or 230, irrelevant
	- y can be thought of as the index of the point array but is irrelevant

Output:
- Number of arrows, arrow position is irrelevant here


---
# Implementation/Approach

We note that a greedy approach can be taken, whereby we simply sort by the Xstart of each balloon in ascending order, then perform a single pass to check for arrow and balloon interval intersection. By sorting and and tracking the smallest Xend of current balloon group, we can solve problem by only dealing with local criteria and information.

By sorting in ascending order of xStart, we don't actually have to account for it after the sort step, because implicitly, the arrow should be able to pass all previous balloons so long as the smallest xEnd of a given group of arrows is not exceeded by the next arrow fired (since in this case we actually won't pass at least one of the previous balloons anymore) 

We also note that the constraint causes there to be at least 1 balloon, we thus account for this before the main pass through algorithm step.

Algorithm:
	- preSort in ascending order of Xstart
	- smallestXEnd = points[0]
	- arrowsNeeded = 1
	- For each points after zero index
		If (balloon[0] <= smallestXend):
			smallestXEnd = min (balloon[1], smallestXEnd)
		Else:
			arrowsNeeded ++
			smallestXend = balloon[1]


rough work
![](Doc_Images/Pasted%20image%2020260810173248.png)

---
# Solution

idk how tf is this so slow
```python
class Solution:
    def findMinArrowShots(self, points: List[List[int]]) -> int:
        
        #preprocess, sort by the ascending Xstart of each point
        points.sort(key=lambda x: x[0])
        print (points)

        #base case
        smallestXEnd = points[0][1]
        arrowsNeeded = 1

        #go through rest of points, if doesn't fit the current subarray, add 1 more arrow
        for index, balloon in enumerate(points):
            if (index != 0):
                if (balloon[0] <= smallestXEnd):
                    smallestXEnd = min(balloon[1], smallestXEnd)
                else:
                    arrowsNeeded += 1
                    smallestXEnd = balloon[1]
                
        return arrowsNeeded
```
![](Doc_Images/Pasted%20image%2020260810173008.png)


Somewhat more simplified solution (without index checking by popping it below hand):
```python
class Solution:
    def findMinArrowShots(self, points: List[List[int]]) -> int: 
        #preprocess, sort by the ascending Xstart of each point
        points.sort()
        #print (points)

        #base case
        smallestXEnd = points[0][1]
        points.pop(0)
        arrowsNeeded = 1

        #go through rest of points, if doesn't fit the current subarray, add 1 more arrow
        for balloon in points:
                if (balloon[0] <= smallestXEnd):
                    smallestXEnd = min(balloon[1], smallestXEnd)
                else:
                    arrowsNeeded += 1
                    smallestXEnd = balloon[1]
                
        return arrowsNeeded
```