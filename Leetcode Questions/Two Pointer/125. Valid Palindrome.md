
### Problem:

A phrase is a **palindrome** if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward. Alphanumeric characters include letters and numbers.

Given a string `s`, return `true` _if it is a **palindrome**, or_ `false` _otherwise_.

**Example 1:**

```
Input: s = "A man, a plan, a canal: Panama"
Output: true
Explanation: "amanaplanacanalpanama" is a palindrome.
```

**Example 2:**

```
Input: s = "race a car"
Output: false
Explanation: "raceacar" is not a palindrome.
```

**Example 3:**

```
Input: s = " "
Output: true
Explanation: s is an empty string "" after removing non-alphanumeric characters.
Since an empty string reads the same forward and backward, it is a palindrome.
```

### Notes:

- Using two pointers, we can compare from either ends until we reach the center
- If not alpha numeric, simply decrement/increment pointers (unless pointers would pass each other)

### Pseudocode:

1. ptrRight ← s.length
2. ptrLeft ← 0
3. WHILE ptrRight ≠ ptrLeft OR ptrLEFT > ptrRight
    1. IF s[ptrRight] ≠ s[ptrLeft]
        1. return False
    2. ptrRight -= 1;
    3. ptrLeft += 1;
4. return True

### Solution
```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        parsed = s.lower()

        ptrRight = len(parsed) - 1
        ptrLeft = 0

        while ((ptrRight != ptrLeft and ptrLeft < ptrRight) and len(parsed) != 0):
            #print (parsed[ptrLeft] + str(ptrLeft) + " " + parsed[ptrRight] + str(ptrRight))

            while (parsed[ptrRight].isalnum() != True and ptrLeft < ptrRight):
                ptrRight -= 1
            while (parsed[ptrLeft].isalnum() != True and ptrLeft < ptrRight):
                ptrLeft += 1

            if (parsed[ptrRight] != parsed[ptrLeft]):
                return False
            ptrRight -= 1
            ptrLeft += 1

        return True
```
![[Pasted image 20260808222208.png]]