
### Problem:

You are given a string `s` and an integer `k`. You can choose any character of the string and change it to any other uppercase English character. You can perform this operation at most `k` times.

Return _the length of the longest substring containing the same letter you can get after performing the above operations_.

**Example 1:**

```
Input: s = "ABAB", k = 2
Output: 4
Explanation: Replace the two 'A's with two 'B's or vice versa.
```

**Example 2:**

```
Input: s = "AABABBA", k = 1
Output: 4
Explanation: Replace the one 'A' in the middle with 'B' and form "AABBBBA".
The substring "BBBB" has the longest repeating letters, which is 4.
There may exists other ways to achieve this answer too.
```

**Constraints:**

- `1 <= s.length <= 105`
- `s` consists of only uppercase English letters.
- `0 <= k <= s.length`

### Notes:

- Use Hashtable and Sliding Window
- Similar to Longest Substring Without Repeating Characters

### Pseudocode:

### Solution:
- BAD SOLUTION
```python
class Solution:
    def characterReplacement(self, s: str, k: int) -> int:
        table = {}
        substringLen = 0

        leftPtr = 0
        rightPtr = 0

        while (rightPtr < len(s)):
            #add to table
            if (s[rightPtr] not in table):
                table[s[rightPtr]] = 1
            else:
                table[s[rightPtr]] += 1

            #check if character replacement results in valid substring
            #get most common occurences + other occurences
            mostCommonOccurences = 0
            otherOccurences = 0
            for x in table: #x = key
                if (table[x] > mostCommonOccurences):
                    otherOccurences += mostCommonOccurences

                    mostCommonOccurences = table[x]
                else:
                    otherOccurences += table[x]
            
            #print (str(mostCommonOccurences) + " " + str(otherOccurences) + " " + str(leftPtr) + " " + str(rightPtr))
            #print (table)

            #decide if replacements results in valid substring or if we need to start shrinking the substring size
            if (otherOccurences <= k):     
                rightPtr += 1

                if (rightPtr - leftPtr > substringLen):
                    substringLen = rightPtr - leftPtr
            else:
                #print ("popping" + str(leftPtr))
                if (table[s[leftPtr]] == 0):
                    table.pop(s[leftPtr])
                else:
                    table[s[leftPtr]] -= 1
                    table[s[rightPtr]] -= 1 #needed to counteract the += 1 for rightPtr above
                leftPtr += 1

            
            
        return substringLen

```
![[Pasted image 20260808221945.png]]
