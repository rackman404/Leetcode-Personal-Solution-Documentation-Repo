


# Sorting
``` python
#sorting a 2d array via a element in array
#in this case each row is sorted by the column element 1, and in descending order
sorted(variable, key = lambda x: x[1], reverse = True)

#generic list sort
variable.sort()
variable.sort(reverse = True)
``` 